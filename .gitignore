*.raw
*.log
__pycache__/
.DS_Store

# sim/ harness: the raw ngspice logs under corners/ (one per PVT point) and
# mc-draws/ (one per Monte Carlo draw and per negative-control draw) are
# committed evidence -- the append-only record trail, see sim/README.md -- so
# they escape the *.log rule above.
!sim/*/corners/**/*.log
!sim/*/mc-draws/**/*.log
# sim/sar-sequencer-behavioral/ (issue #55): a standalone behavioral
# testbench outside the shared PVT/MC harness (see that experiment's own
# run_testbench.py module docstring), whose raw ngspice transient log is
# committed evidence the same way corners/ and mc-draws/ are above.
!sim/*/runs/*.log
# harness scratch: regenerated netlists and generated corner decks. The
# runners themselves simulate in a TemporaryDirectory and only copy the
# resulting logs back under sim/<experiment>/, so this only catches
# interactive/xschem scratch (see sim/xschemrc's netlist_dir).
sim/build/
# ngspice model-check scratch dumps (e.g. bsim4v5.out) from ad hoc ngspice
# invocations at repo root. Root-anchored so it doesn't clobber unrelated
# *.out files elsewhere in the tree -- notably sim/smoke_test.ngspice.out,
# which IS committed evidence (see sim/README.md "Bootstrap smoke test").
/*.out

# layout/ DRC/LVS flow: local klt install, created by layout/bin/setup-venv.sh
layout/.venv/
# >>> loom-managed (do not edit) >>>
# Loom runtime state (don't commit these)
.loom-in-use
.loom-checkpoint
.loom-managed
.no-changes-needed
.snapshots/
.loom/.daemon.pid
.loom/.daemon.log
.loom/daemon.sock
.loom/daemon-loop.pid
.loom/daemon-metrics.json
.loom/loom-source-path
.loom/spawn-loop-state.json
.loom/spawn-loop.pid
.loom/stop-spawn-loop
.loom/issue-failures.json
.loom/interventions/
.loom/worktrees/
.loom/worktrees-local/
.claude/worktrees/
.loom/state.json
.loom/mcp-command.json
.loom/activity.db
.loom/claims/
.loom/locks/
.loom/signals/
.loom/status/
.loom/retry-state/
.loom/exit-codes/
.loom/sweep-checkpoint/
.loom/sweep-run/
.loom/stats/
.loom/diagnostics/
.loom/guide-docs-state.json
.loom/metrics_state.json
.loom/manifest.json
.loom/stuck-config.json
.loom/metrics/
.loom/usage-cache.json
.loom/claude-config/
.loom/tokens/
.loom/accounts.env
.loom/account-health.json
.loom/account-health.lock
.loom/CANARY
.loom/*.log
.loom/*.sock
.loom/*.tmp
.loom/*.bak
.loom/.install.lock
.loom/logs/
.loom/.resync-in-progress
# <<< loom-managed <<<

# Repo Skills machine-local install metadata (absolute source path + timestamp)
.claude/skills/repo/.install-local.json
