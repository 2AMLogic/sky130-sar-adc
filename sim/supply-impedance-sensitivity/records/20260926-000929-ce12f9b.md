# Record 20260926-000929-ce12f9b

- **Record ID**: 20260926-000929-ce12f9b
- **Claim**: `spec/target-spec.md#target-table` -- **Power** (DRAFT row), INFORMATIONAL only, and the evidence `spec/decision-records/DR-015-package-parasitic-assumption.md` needs to support a sensitivity claim it currently makes in prose: that the `no-gnd-pad` arm -- DR-012's REJECTED null option -- is "*entirely* a function of `R_SUB`: with a small `R_SUB` it looks harmless, with a large one it looks fatal". It edits no spec row, proposes no power target, and grades nothing against a ratified line.
- **Netlist provenance**: schematic (`design/sar_adc_top.spice`), with two TESTBENCH-ONLY transformations that are never written back to `design/`: the DUT's `GND` net is renamed `GND_DIE` (a net named `GND` is ngspice's global node 0 and would short out every series element this campaign inserts), and the committed fragment's `VVDD`/`VVPWR`/`VVGND` source cards are re-pointed to board-side nodes. Source instance names, the `.tran` card, the clock/reset/input schedule and every code and phase `.meas` card are used verbatim.
- **Corner matrix run**: process=['tt'], temperature_c=[27.0], supply_v=[1.8] (1 points, one-at-a-time per sim/README.md)
- **Ladder**: 3 substrate-return resistances plus the `ideal` control, at 1 corner point(s) = 4 whole-ADC transients. Every swept point is the `no-gnd-pad` topology (`GND` unbonded; `VDD`/`VPWR`/`VGND` at DR-015's R+L) with exactly one element moved.
- **Stimulus**: `sim/full-conversion-transient/testbench/full_conversion_tb_fragment.spice`, unmodified except for the supply source cards' nodes -- `f_clk = 12 MHz` (DR-006 worst case), 6 back-to-back conversions of 12 CLK periods, the first discarded as start-up, the remaining five carrying DC differential inputs of `-0.78*V_REF`, `-0.25*V_REF`, `+0.00*V_REF`, `+0.25*V_REF`, `+0.78*V_REF`.

## Why this record exists

[DR-015](../../../spec/decision-records/DR-015-package-parasitic-assumption.md)'s Consequences section says, of the arm that implements DR-012's *rejected* option: "The `no-gnd-pad` arm ... is **entirely** a function of `R_SUB`: with a small `R_SUB` it looks harmless, with a large one it looks fatal." Until this record that was an argument, not a measurement -- the campaign had run that topology at exactly one substrate magnitude (30 Ohm), so "harmless" and "fatal" were both extrapolations from a single point. This record is the bounded ladder that turns the sensitivity into a number, at the baseline corner, and it is the residual of [issue #409](https://github.com/2AMLogic/sky130-sar-adc/issues/409)'s third item that the 2-D box did not reach.

**It supersedes nothing, and is not a re-run of the 2-D sweep.** That box moved a substrate resistance on the as-built `package` topology, where `GND` is bonded through ~102 mOhm and the substrate resistor is a secondary shunt between two ground die nodes. Here the SAME element carries the analog ground's entire return current, because there is no analog ground bond at all. Same constant, same decade, structurally different experiment -- and the reason the earlier record's own scope section said an `R_SUB` sweep was still owed.

## Which stand-in this ladder moves, and what it is called

DR-015 item 2 defines two lumped stand-ins: `R_SUB`, a substrate-only ground RETURN, and `R_SUBX`, the link "between the analog and digital ground die nodes". In the `no-gnd-pad` topology **one resistor is both**: it spans `GND_DIE` and `VGND` (so the deck names its instance `RSUBX`, after the node pair it bridges) and, with `GND` unbonded, it is also the only path the analog ground has to the board (so it plays `R_SUB`'s role). DR-015 sets both stand-ins to the same 30 Ohm, so at the anchor point the distinction changes no number -- but a record that swept "the substrate resistance" without saying which element moved would be unreadable against the 2-D box, which swept an element with the same name in a topology where it does something else. What moved here is the single resistor named in every deck under this record, and nothing else.

## The package-style assumption (DR-015), and which of it is swept

| element | value | where it comes from |
|---|---|---|
| bond-wire inductance | 1.414 nH | straight round-wire self-inductance `L = (mu0*l/2pi)(ln(2l/r) - 3/4)` for l = 1.5 mm, d = 25.4 um (computed in `run_supply_impedance.py`, not quoted from anywhere) |
| bond-wire resistance | 72.2 mOhm | `rho*l/A` for the same wire, gold `rho = 2.44e-08` Ohm*m |
| lead-frame + board allowance | 0.5 nH, 30 mOhm | a STATED allowance for the path between the package pin and the board star point -- an assumption, not a derivation (DR-015) |
| **per-terminal total** | **102.2 mOhm + 1.914 nH** | the `package` / `no-gnd-pad` arms' series impedance on each bonded terminal |
| substrate stand-in `R_SUB` | 30 Ohm | a LUMPED stand-in, magnitude from DR-012's own "10s of ohms of p-substrate" wording. NOT an extracted substrate network -- see the caveat below |
| substrate link `R_SUBX` | 30 Ohm | lumped stand-in for the path that makes `GND` and `VGND` one extracted net (DR-012); present in every arm, carrying no current in `ideal` |

The ladder moves **only** the lumped substrate row. The per-terminal bond R+L is held at DR-015's stated 102.2 mOhm / 1.914 nH on all three bonded terminals throughout, so each step changes one element and nothing else (DR-015 item 5).

## The swept points, as networks

| arm | `VDD` | `GND` | `VPWR` | `VGND` | what it isolates |
|---|---|---|---|---|---|
| `ideal` | ideal source at the die | ideal source at the die | ideal source at the die | ideal source at the die | every supply terminal driven by an ideal source at the die node (the zero-impedance control every existing sim/ campaign runs today) |
| `nullsweep-rsub3` | R = 102.2 mOhm + L = 1.914 nH | **no bond** (substrate only) | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | DR-012's rejected `no-gnd-pad` topology (GND unbonded) with its lumped substrate return at 3 Ohm; the other three terminals keep DR-015's R+L unchanged |
| `nullsweep-rsub30` | R = 102.2 mOhm + L = 1.914 nH | **no bond** (substrate only) | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | DR-012's rejected `no-gnd-pad` topology (GND unbonded) with its lumped substrate return at 30 Ohm; the other three terminals keep DR-015's R+L unchanged |
| `nullsweep-rsub300` | R = 102.2 mOhm + L = 1.914 nH | **no bond** (substrate only) | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | DR-012's rejected `no-gnd-pad` topology (GND unbonded) with its lumped substrate return at 300 Ohm; the other three terminals keep DR-015's R+L unchanged |

The `ideal` row is the control every delta below is taken against: ideal sources at the die, the zero-impedance case every other `sim/` campaign runs. Each swept row carries its own lumped resistor between the analog and digital ground DIE nodes, which is the swept axis; in `ideal` both ends of that resistor are held at 0 V, so it carries no current and the control stays a true zero-impedance reference.

## Die-side analog-ground excursion along the ladder

| lumped substrate return | `GND_DIE` pp | `VGND` pp | worst mid-scale \|delta code\| vs `ideal` |
|---|---|---|---|
| **3 Ohm** | 72.130 mV | 72.288 mV | 0 LSB |
| **30 Ohm** (anchor) | 67.307 mV | 71.425 mV | 0 LSB |
| **300 Ohm** | 137.093 mV | 57.388 mV | 0 LSB |

Peak-to-peak over the same steady-state conversion the fragment averages its supply currents over. The `ideal` control's own `GND_DIE` value is 0.000 mV -- mechanically zero, because an ideal source holds the die node at 0 V; a nonzero value there would mean the networks are not wired as stated. The two `+-0.78*V_REF` inputs are EXCLUDED from the delta column (they are reported per point in the table below): `sim/full-conversion-transient/records/20260912-002315-9aaf1ca.md` already records them as wrong by ~100 LSB at every corner (issue #267, common-mode saturation at large differential input, still open), so a change there could not be attributed to supply impedance.

## Captured code per point

| point | `-0.78*V_REF` (ideal 113) | `-0.25*V_REF` (ideal 384) | `+0.00*V_REF` (ideal 512) | `+0.25*V_REF` (ideal 640) | `+0.78*V_REF` (ideal 911) | worst \|delta code\| vs `ideal` (mid-scale) |
|---|---|---|---|---|---|---|
| `ideal` | 214 | 383 | 511 | 641 | 1023 | -- (control) |
| `nullsweep-rsub3` | 214 (+0) | 383 (+0) | 511 (+0) | 641 (+0) | 1023 (+0) | 0 |
| `nullsweep-rsub30` | 214 (+0) | 383 (+0) | 511 (+0) | 641 (+0) | 1023 (+0) | 0 |
| `nullsweep-rsub300` | 214 (+0) | 383 (+0) | 511 (+0) | 641 (+0) | 1023 (+0) | 0 |

## Rail excursion and average current per point

| point | gnd_die pp (mV) | vgnd_die pp (mV) | vdd_die pp (mV) | vpwr_die pp (mV) | I(VDD) (uA) | I(VPWR) (uA) | I(GND) (uA) | total power (uW) |
|---|---|---|---|---|---|---|---|---|
| `ideal` | 0.000 | 0.000 | 0.000 | 0.000 | 2.097 | 6.966 | 2.177 | 27.971 |
| `nullsweep-rsub3` | 72.130 | 72.288 | 26.999 | 50.895 | 2.121 | 6.518 | n/a (no bond) | 27.268 |
| `nullsweep-rsub30` | 67.307 | 71.425 | 25.449 | 49.437 | 2.123 | 6.543 | n/a (no bond) | 27.315 |
| `nullsweep-rsub300` | 137.093 | 57.388 | 23.619 | 47.551 | 2.134 | 6.571 | n/a (no bond) | 27.384 |

`I(GND)` is `n/a (no bond)` on every swept row by construction: this topology's whole point is that the analog ground has no bond current to measure, because it has no bond.

## Findings

- **The ladder is anchored to the committed ground-pad ablation.** Its `nullsweep-rsub30` point is, card for card, the `no-gnd-pad` arm this campaign already recorded at this corner: same three bonded terminals at DR-015's R+L, same unbonded `GND`, same stimulus, same lumped resistor at DR-015's assumed 30 Ohm. Checked at record-write time (`null_sweep_anchor_matches_base_arm()`), so the ladder is tied to that record rather than merely described as being.
- **Die-side analog-ground excursion along the ladder**: 72.130 mV -> 67.307 mV -> 137.093 mV as the lumped substrate return goes 3 Ohm -> 30 Ohm -> 300 Ohm. Only that resistor moves, so the change is the substrate return's own contribution and nothing else (DR-015 item 5).
- **No mid-scale captured code moves anywhere in this ladder.** Every point reproduces the `ideal` control's mid-scale codes exactly (worst |delta code| = **0 LSB**) across 3-300 Ohm of lumped substrate return. That is a **bounded null result** for DR-012's rejected option: the magnitude at which deleting the analog ground pad would cost a code is **outside** this ladder, not located inside it, and a wider ladder (or another corner) could still find one. It is specifically NOT a finding that the pad does not matter -- the excursion row above moves even where the code row does not.
- **Worst die-side analog-ground excursion on the ladder**: **137.093 mV** peak-to-peak (38.995 LSB at the nominal supply) at a 300 Ohm substrate return. Undecoupled by construction (DR-015 item 6): this design has no on-die decoupling and none is modelled, so the figure is an upper bound rather than a prediction.
- **How sensitive the rejected option actually is to the assumption**: a 100x change in the lumped substrate return (from 3 to 300 Ohm) moves the die-side analog-ground excursion by 1.90x (72.130 mV -> 137.093 mV). This is the measured version of DR-015's own prose claim that the arm is "*entirely* a function of `R_SUB`" -- which was, until this record, an argument from one point.

## Wall-clock cost per run

| point | wall clock (s) | x the `ideal` control |
|---|---|---|
| `ideal` | 375 | 1.00x |
| `nullsweep-rsub3` | 799 | 2.13x |
| `nullsweep-rsub30` | 682 | 1.82x |
| `nullsweep-rsub300` | 679 | 1.81x |

Reported because this campaign's cost history is itself evidence: this topology was left unrun across several passes on a truncated-slice projection of roughly an order of magnitude that a full run then falsified (1.67x the control, `records/20260925-204633-7339971.md`). These are wall-clock seconds on a shared, contended host, so they are ratios between points rather than a benchmark.

## What this ladder does not cover

Stated for the same reason `sim/README.md` requires a corner subset to be justified: a reader must not have to infer which questions it leaves open.

- **No extracted substrate network** (issue #409's fourth item, and DR-015's own still-open one). The swept element is a single lumped resistor standing in for a distributed, layout-dependent thing, with no `klt extract` behind it. Sweeping a stand-in over two decades bounds the *sensitivity* to it -- which is exactly what this record is for -- but it does not turn any point of the ladder into a measurement of this die's substrate, and no number here may be quoted as one.
- **The bond inductance is not crossed with this axis.** All three bonded terminals stay at DR-015's R+L throughout, so this is a one-axis ladder and not a second 2-D box. A point here is therefore a statement about the substrate return at DR-015's bond, not at an arbitrary one.
- **No decoupling, on-die or on-board** (DR-015 item 6, carried from DR-010 and DR-012). Every point here is the undecoupled case.
- **The two near-full-scale inputs** are outside every code comparison above, for the reason stated under the excursion table (issue #267).
- **The ladder is bounded, and a null inside it is not a null outside it.** The substrate return is swept over 3-300 Ohm at one corner. Nothing here states what happens beyond those edges.

## Subset-corner justification (`sim/README.md`)

This record runs the null-option substrate ladder at 1 point(s) of the ratified corner set (`tt_27c_1.80v`), not all nine. Three separate constraints bind, and none of them is a judgement that the corners do not matter:

- **Host policy.** The machine this record was produced on is a shared dispatch worker whose operating rules forbid running a multi-corner ngspice grid locally; a grid there must be expressed as a `klt sim` request and submitted to an EDA batch fleet. Sequential single-corner runs are the shape those rules do allow, and that is what the table above is.
- **The batch route cannot mint a record in THIS repo's format.** `klt sim` owns its own request/response JSON contract and its own corner expansion; every record under `sim/` is written by this repo's `sim/harness/evidence.py` against a deck this repo assembles. Routing the grid through `klt sim` would produce a different artefact, not this one. Separately, the fleet's runner image installs ngspice from the distribution archive, and this repo's own CI already records what that means: `.github/workflows/ci.yml` states that the archive build is **ngspice-42**, below `sim/toolchain.json`'s `ngspice_min_major = 46` floor, which is why CI builds ngspice from source instead. A record minted below that floor is refused by `sim/check_spec_coverage.py`'s pin gate and would not be comparable with anything already under `sim/`.
- **Cost.** The nine-point ratified grid across the 4 decks of this record is 36 whole-ADC transients. At the per-run cost measured above that is a campaign in its own right, not a longer version of this one.

So the ratified-grid run is **deferred, not skipped**: the runner already implements it (`--corners`, the nine-point `ratified_oat_grid()` x the decks) and this experiment's README names the exact command. What it needs is a host whose ngspice satisfies the pin and whose policy allows a grid -- not more code. Until then, no statement in this record is a corner-worst-case claim; it is a bounded sensitivity ladder at the baseline corner.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `ce12f9b411fda9e76ec5ed6827a2b00800f27fb4` on `feature/issue-409` (dirty)
- DUT netlist sha256: `96b3696ee9ecc84417c44f4bda51584e6a2cdd8d94c3ce9c4393993aef6c481f`
- tran step: 0.5 ns
- simulated span: 6283.3 ns per run
- runs: 4 (3 swept points + the `ideal` control x 1 corner point)
- testbench fragment sha256: `d819f87031897aba361e038be1e5bffab97f9c8a3f88b6770d0575606c5fdb01`
- swept ladder: `no-gnd-pad` topology; lumped substrate return in {3, 30, 300} Ohm (DR-015 assumes 30); bond R+L fixed at 102.2 mOhm / 1.914 nH on VDD/VPWR/VGND (DR-015)

- **Supersedes**: (none)

Written by `sim/supply-impedance-sensitivity/run_supply_impedance.py --null-sweep --record`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).

