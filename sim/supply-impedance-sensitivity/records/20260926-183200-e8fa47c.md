# Record 20260926-183200-e8fa47c

- **Record ID**: 20260926-183200-e8fa47c
- **Claim**: `spec/target-spec.md#target-table` -- **Power** (DRAFT row), INFORMATIONAL only, and the evidence `spec/decision-records/DR-017-on-die-decoupling-budget.md`'s routing-parasitics open item ("the sign of the net effect is still unmeasured", issue #465) asks for. It edits no spec row, proposes no power target, and grades nothing against a ratified line. What it measures is whether the interconnect resistance the DRAWN decoupling ties add -- measured in `layout/sar-adc-top/`, never yet present in any netlist here -- raises or lowers the die-side rail excursion.
- **Netlist provenance**: schematic (`design/sar_adc_top.spice`), with THREE testbench-only transformations that are never written back to `design/`: the DUT's `GND` net is renamed `GND_DIE` (a net named `GND` is ngspice's global node 0 and would short out every series element this campaign inserts); the committed fragment's `VVDD`/`VVPWR`/`VVGND` source cards are re-pointed to board-side nodes; and on every rung above `0x`, each decoupling capacitor's two plate nodes are re-pointed behind a resistor carrying that tie's own MEASURED resistance. Source instance names, the `.tran` card, the clock/reset/input schedule and every code and phase `.meas` card are used verbatim, and the `0x` rung's deck contains none of the third transformation at all.
- **Corner matrix run**: process=['tt'], temperature_c=[27.0], supply_v=[1.8] (1 points, one-at-a-time per sim/README.md)
- **Ladder**: 2 rungs of the measured tie resistance (0x, 1x) plus the `ideal` control, at 1 corner point(s) = 3 whole-ADC transients. Every rung is the as-built `package` topology with exactly those four resistors moved.
- **Stimulus**: `sim/full-conversion-transient/testbench/full_conversion_tb_fragment.spice`, unmodified except for the supply source cards' nodes -- `f_clk = 12 MHz` (DR-006 worst case), 6 back-to-back conversions of 12 CLK periods, the first discarded as start-up, the remaining five carrying DC differential inputs of `-0.78*V_REF`, `-0.25*V_REF`, `+0.00*V_REF`, `+0.25*V_REF`, `+0.78*V_REF`.

## Why this record exists

[DR-017](../../../spec/decision-records/DR-017-on-die-decoupling-budget.md) put one `cap_mim_m3_1` pair per supply domain into the design, and its Amendment A argues about ESR on the strength of the DEVICE's own plate resistance (0.0235 Ohm for the shipped pair). Issue #440 then drew the ties and measured what the INTERCONNECT adds: about 590x that, and ~80 % of it single-cut vias. A 2x2 via array at each riser and each plate entry would cut it roughly 3x and is cheap in area -- but #440 declined to draw one, because the sign of the benefit is not obvious. At the as-built resistance the resonance the pair forms with DR-015's bond inductance has `Q ~ 1.06`, so the ties are comparable to the pair's own reactance there and are DAMPING that resonance; removing them raises `Q` as much as it lowers the impedance at resonance. Read the other way, ESR does not reach the pair's reactance until ~1.3 GHz, so the pair should already be reactance-dominated where the bounce lives. Both readings are design-time inferences about the same four resistors. This record replaces them with a measurement, and it is [issue #465](https://github.com/2AMLogic/sky130-sar-adc/issues/465). It supersedes nothing: the arm comparison ranks five networks at DR-015's assumption point, the sweeps walk boxes around it, and this ladder holds the network fixed and moves a resistance none of them contained.

## The measured decoupling ties (issue #440)

| drawn tie | lumped resistance | domain | domain ESR | Q at the package resonance | ESR = X_C at |
|---|---|---|---|---|---|
| `GND (analog return -> both bottom plates)` | 10.857 Ohm | analog (VDD/GND) | **13.837 Ohm** | 1.062 | 1.297 GHz |
| `VDD (analog supply -> both top plates)` | 2.980 Ohm | analog (VDD/GND) | **13.837 Ohm** | 1.062 | 1.297 GHz |
| `VGND (digital return -> both bottom plates)` | 9.843 Ohm | digital (VPWR/VGND) | **13.448 Ohm** | 1.092 | 1.334 GHz |
| `VPWR (digital supply -> both top plates)` | 3.605 Ohm | digital (VPWR/VGND) | **13.448 Ohm** | 1.092 | 1.334 GHz |

Each domain's ESR is its two ties in series, carrying its 8.870 pF pair. `Q` and the crossover are the probe's own, at the 1221.5 MHz resonance that capacitance forms with DR-015's per-terminal 1.914 nH; the pair's reactance there is 14.69 Ohm (analog (VDD/GND)), 14.69 Ohm (digital (VPWR/VGND)).

**These are a measurement, not an assumption** -- unlike DR-015's R+L below. Their provenance is a committed, append-only layout record (`layout/sar-adc-top/reports/20260926-081248-203cca3`), and this runner reads the ohms out of that record's own `decap-ties.json` rather than carrying a copy, so a record here cannot state a resistance the layout never had. What they are NOT: an extraction. Each is one lumped DC resistor assembled from `build_layout.py`'s own geometry constants and the PDK's sheet/via resistances (`res_typical__cap_typical.spice`), with no frequency dependence, no skin effect, and **no interconnect inductance** -- that half of DR-017's routing-parasitics item needs `klt pex` and the device/net correspondence this flow does not yet have (klayout-tools#1878), and is out of scope here.

## The package-style assumption (DR-015), which this ladder holds fixed

| element | value | where it comes from |
|---|---|---|
| bond-wire inductance | 1.414 nH | straight round-wire self-inductance `L = (mu0*l/2pi)(ln(2l/r) - 3/4)` for l = 1.5 mm, d = 25.4 um (computed in `run_supply_impedance.py`, not quoted from anywhere) |
| bond-wire resistance | 72.2 mOhm | `rho*l/A` for the same wire, gold `rho = 2.44e-08` Ohm*m |
| lead-frame + board allowance | 0.5 nH, 30 mOhm | a STATED allowance for the path between the package pin and the board star point -- an assumption, not a derivation (DR-015) |
| **per-terminal total** | **102.2 mOhm + 1.914 nH** | the `package` / `no-gnd-pad` arms' series impedance on each bonded terminal |
| substrate stand-in `R_SUB` | 30 Ohm | a LUMPED stand-in, magnitude from DR-012's own "10s of ohms of p-substrate" wording. NOT an extracted substrate network -- see the caveat below |
| substrate link `R_SUBX` | 30 Ohm | lumped stand-in for the path that makes `GND` and `VGND` one extracted net (DR-012); present in every arm, carrying no current in `ideal` |

Every row above is identical on every rung. The ladder moves only the four measured tie resistors, so the difference between two rungs is their own contribution and nothing else (DR-015 item 5). The `R_SUB` row does not appear in this ladder's decks at all: the as-built `package` topology bonds all four terminals, and only the `substrate`/`no-gnd-pad` arms carry that element.

## The rungs, as networks

| arm | `VDD` | `GND` | `VPWR` | `VGND` | what it isolates |
|---|---|---|---|---|---|
| `ideal` | ideal source at the die | ideal source at the die | ideal source at the die | ideal source at the die | every supply terminal driven by an ideal source at the die node (the zero-impedance control every existing sim/ campaign runs today) |
| `decapesr-0x` | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | as-built `package` topology with NO decoupling-tie interconnect resistance -- the committed netlist card for card, which is the idealisation every decoupled record in this campaign has measured |
| `decapesr-1x` | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | R = 102.2 mOhm + L = 1.914 nH | as-built `package` topology with 1x the MEASURED drawn decoupling-tie resistance (analog (VDD/GND) 13.837 Ohm, digital (VPWR/VGND) 13.448 Ohm per domain, in series with that domain's own MiM pair) from `layout/sar-adc-top/reports/20260926-081248-203cca3` |

The `ideal` row is the control every code delta below is taken against: ideal sources at the die, the zero-impedance case every other `sim/` campaign runs. Its own deck carries no tie resistors either -- the third transformation is applied per rung, and the control is not a rung. Every rung carries the same lumped `R_SUBX = 30 Ohm` between the analog and digital ground die nodes; in `ideal` both of its ends are held at 0 V, so it carries no current.

## Die-side rail excursion per rung

| rung | tie resistance per domain | gnd_die pp (mV) | vgnd_die pp (mV) | vdd_die pp (mV) | vpwr_die pp (mV) |
|---|---|---|---|---|---|
| `ideal` (control) | -- (no bond, no ties) | 0.000 | 0.000 | 0.000 | 0.000 |
| `decapesr-0x` | none | 9.854 | 11.326 | 9.156 | 12.135 |
| `decapesr-1x` | analog (VDD/GND) 13.837 Ohm, digital (VPWR/VGND) 13.448 Ohm | 9.975 | 12.658 | 10.123 | 16.180 |

`pp` is the peak-to-peak excursion of the DIE-side node over the same steady-state conversion the fragment averages its supply currents over. The `ideal` row is the mechanical control: an ideal source holds each die node at its own DC value, so a nonzero ground excursion there would mean the networks are not wired the way this record says.

## Captured code per rung

| rung | `-0.78*V_REF` (ideal 113) | `-0.25*V_REF` (ideal 384) | `+0.00*V_REF` (ideal 512) | `+0.25*V_REF` (ideal 640) | `+0.78*V_REF` (ideal 911) | worst \|delta code\| vs `ideal` (mid-scale) |
|---|---|---|---|---|---|---|
| `ideal` | 214 | 383 | 511 | 641 | 1023 | -- (control) |
| `decapesr-0x` | 214 (+0) | 383 (+0) | 511 (+0) | 641 (+0) | 1023 (+0) | 0 |
| `decapesr-1x` | 214 (+0) | 383 (+0) | 511 (+0) | 641 (+0) | 1023 (+0) | 0 |

The two `+-0.78*V_REF` columns are reported but EXCLUDED from the worst-delta column: `sim/full-conversion-transient/records/20260912-002315-9aaf1ca.md` already records them as wrong by ~100 LSB at every corner (issue #267, common-mode saturation at large differential input, still open), so a change there could not be attributed to a decoupling tie.

## Average rail current per rung

| rung | I(VDD) (uA) | I(VPWR) (uA) | I(GND) (uA) | I(VREFP) (uA) | I(VCM) (uA) | total power (uW) |
|---|---|---|---|---|---|---|
| `ideal` | 2.097 | 6.966 | 2.177 | 6.470 | 0.014 | 27.971 |
| `decapesr-0x` | 2.114 | 6.555 | 2.200 | 6.502 | 0.012 | 27.319 |
| `decapesr-1x` | 2.123 | 6.544 | 2.207 | 6.503 | 0.011 | 27.317 |

## Findings

- **The ladder is anchored to the committed decoupled netlist.** Its `decapesr-0x` rung emits no tie resistors at all, so it is card for card the `package` arm this campaign has already recorded on the same DUT: same per-terminal R+L, same lumped `R_SUBX = 30 Ohm`, same stimulus, same device cards. Checked before the run and again at record-write time (`decap_esr_anchor_matches_base_arm()`), on the arm network AND on the patched DUT body, so the ladder is tied to the existing records rather than merely described as being.
- **Where the resistance comes from**: `layout/sar-adc-top/reports/20260926-081248-203cca3/decap-ties.json`, issue #440's measurement of the DRAWN ties -- analog (VDD/GND) 13.837 Ohm, digital (VPWR/VGND) 13.448 Ohm per domain, read out of that record by this runner rather than typed in. It is a lumped DC ladder over the drawn straps and vias from the PDK's own sheet/via resistances: no frequency dependence, and **no interconnect inductance** (the other half of DR-017's routing-parasitics item, which needs `klt pex`, is not modelled here).
- **analog ground (`GND`, the comparator's own reference)**: peak-to-peak die-side excursion 9.854 mV -> 9.975 mV as the drawn tie resistance goes 0x -> 1x. Only those four resistors move along this row.
- **digital ground (`VGND`)**: peak-to-peak die-side excursion 11.326 mV -> 12.658 mV as the drawn tie resistance goes 0x -> 1x. Only those four resistors move along this row.
- **analog supply (`VDD`)**: peak-to-peak die-side excursion 9.156 mV -> 10.123 mV as the drawn tie resistance goes 0x -> 1x. Only those four resistors move along this row.
- **digital supply (`VPWR`)**: peak-to-peak die-side excursion 12.135 mV -> 16.180 mV as the drawn tie resistance goes 0x -> 1x. Only those four resistors move along this row.
- **The sign, which is what this record exists to establish**: adding the measured drawn tie resistance to the committed netlist takes the worst die-side rail excursion from **12.135 mV** to **16.180 mV** peak-to-peak (1.333x), per rail analog ground 1.012x, digital ground 1.118x, analog supply 1.106x, digital supply 1.333x, at a stated reading band of +-2 %. Worst mid-scale |delta code| between the two rungs = **0 LSB**. Direction: **amplifying**.
- **What that means for the drawn geometry**: **cutting the tie resistance is directionally worth something.** The drawn ties RAISE the die-side excursion, so a 2x2 via array at each riser and each plate entry -- which #440 projected at roughly a 3x reduction, to ~4-5 Ohm per domain -- would recover part of that difference. How much is NOT measured here: this ladder brackets the reachable value between its two rungs rather than running the projected array geometry itself, and the arrays cannot reach the `0x` rung (they can only reduce the via half of the ladder, and the metal half stays). One corner (`tt_27c_1.80v`), DR-015's assumed package magnitudes, no board decoupling, and a DC-only tie model -- so this is a directional finding about this stimulus at this corner, not a universal statement about decoupling ESR.
- **No mid-scale captured code moves on this ladder.** Every rung reproduces the `ideal` control's mid-scale codes exactly (worst |delta code| = **0 LSB**), so the tie resistance changes the rails' own excursion without changing a captured code at this corner -- a bounded null result on the code axis, not a null on the excursion axis.

## Wall-clock cost per run

| rung | wall clock (s) | x the `ideal` control |
|---|---|---|
| `ideal` | 967 | 1.00x |
| `decapesr-0x` | 3582 | 3.71x |
| `decapesr-1x` | 2077 | 2.15x |

Reported for the same two reasons every record of this campaign reports it: it is the input to the subset-corner justification below, and it is itself a finding -- this campaign's expensive shape is the bonded one, and the `package` arm has repeatedly outrun a ~63-minute per-process budget on this fleet (issue #448), which is why this ladder has two rungs rather than four. These are wall-clock seconds on a shared, contended host, so they are ratios between rungs rather than a benchmark of either.

## What this ladder does not cover

- **The interconnect INDUCTANCE of the same ties.** The other half of DR-017's routing-parasitics item. It needs an extraction (`klt pex`) that presumes a device/net correspondence this flow does not yet have (klayout-tools#1878, tracked with #103's LVS gap), so these decks model resistance only.
- **The 2x2-via-array geometry itself.** This ladder brackets the reachable value between `0x` and the as-built `1x`; it does not run the projected ~4-5 Ohm array ESR as its own rung. The arrays could never reach `0x` in any case -- they divide the via half of each ladder, and the metal half (10.857 Ohm of the analog return tie is mostly drawn met2) stays.
- **Other corners.** One corner point, for the cost reason stated below. The full ratified grid at this ladder would be 3 x 9 whole-ADC transients.
- **Board decoupling**, which no record of this campaign models, so every excursion here remains an upper bound rather than a prediction for a real system.
- **The two near-full-scale inputs**, outside every code comparison above for the reason stated under that table (issue #267).

## Subset-corner justification (`sim/README.md`)

This record runs the decoupling-tie ESR ladder at 1 point(s) of the ratified corner set (`tt_27c_1.80v`), not all nine. Two constraints bind, and neither of them is a judgement that the corners do not matter:

- **Cost, against a session that must end.** The nine-point ratified grid across the 3 decks of this record is 27 whole-ADC transients at the per-run wall clock measured above -- hours, and the arithmetic is in the table rather than in a projection. The host is a dispatch worker on which an agent session may run ngspice locally (every run above did) but on which **no process may outlive the session that started it** (`.loom/docs/long-running-compute.md`); the sanctioned answer there is to scope the run to the session and land the increment, which is what this record is. `--corner-points` runs a named subset of the same ratified grid and `--log-cache` carries finished points across sessions, so the grid is completed by accumulation -- each session adding the points it can afford -- rather than needing one sitting long enough to hold all of it.
- **The batch route cannot mint a record in THIS repo's format.** `klt sim` owns its own request/response JSON contract and its own corner expansion; every record under `sim/` is written by this repo's `sim/harness/evidence.py` against a deck this repo assembles. Routing the grid through `klt sim` would produce a different artefact, not this one. Separately, the fleet's runner image installs ngspice from the distribution archive, and this repo's own CI already records what that means: `.github/workflows/ci.yml` states that the archive build is **ngspice-42**, below `sim/toolchain.json`'s `ngspice_min_major = 46` floor, which is why CI builds ngspice from source instead. A record minted below that floor is refused by `sim/check_spec_coverage.py`'s pin gate and would not be comparable with anything already under `sim/`.
- **What is NOT a constraint, and used to be stated as one.** The host's ngspice satisfies `sim/toolchain.json`'s floor (the environment block below names the exact build), and local multi-corner simulation is not forbidden here -- the points above were run locally, one at a time, in a single session. Records before issue #409 item 1 stated a blanket host-policy ban; that reason was re-checked on a measuring host and retired.

So the ratified-grid run is **partially done and still open**, not skipped: the runner implements the whole of it (`--corners`, the nine-point `ratified_oat_grid()` x the decks), this experiment's README names the exact command, and what is owed is the eight non-baseline points, tracked in issue #409. Until they are in a record, no statement here is a corner-worst-case claim; it is a one-element sign measurement at the baseline corner.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-46
- Harness: sim/harness 0.1.0
- git: `e8fa47cb12010e84ddaedf93089b407ae55a502f` on `feature/issue-465` (dirty)
- DUT netlist sha256: `6a0be472ef845710dbdce0aa68c4e6ede8c4894c1e0368fe19281a87bc926b7f`
- tran step: 0.5 ns
- simulated span: 6283.3 ns per run
- runs: 3 (2 rungs + the `ideal` control x 1 corner point)
- testbench fragment sha256: `d819f87031897aba361e038be1e5bffab97f9c8a3f88b6770d0575606c5fdb01`
- decoupling-tie measurement: layout/sar-adc-top/reports/20260926-081248-203cca3/decap-ties.json -- analog (VDD/GND) 13.837 Ohm, digital (VPWR/VGND) 13.448 Ohm per domain, 8.870 pF per domain (issue #440)
- tie-resistance ladder: {0x, 1x} of the measured value
- verdict: amplifying at a +-2 % reading band

- **Supersedes**: (none)

Written by `sim/supply-impedance-sensitivity/run_supply_impedance.py --decap-esr --record`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).


## Erratum (issue #465 PR review, 2026-09-26): one number above names the wrong quantity

**Appended, not edited** -- `sim/README.md`'s append-only rule forbids editing a
minted record even for a correction, so the wrong sentence is left standing above
and corrected here. Nothing measured moves: no verdict, corner, excursion, hash
or environment field in this record changes, and the sentence's own point is
right.

Under "What this ladder does not cover", the bullet **The 2x2-via-array geometry
itself** reads:

> The arrays could never reach `0x` in any case -- they divide the via half of
> each ladder, and the metal half (10.857 Ohm of the analog return tie is mostly
> drawn met2) stays.

**10.857 Ohm is the WHOLE analog return tie**, not its metal-only portion: it is
`ties["GND (analog return -> both bottom plates)"].tie_ohm` in the very
`decap-ties.json` this record's environment block cites, and that tie is
emphatically *not* mostly met2 -- ~80 % of it is single via cuts, as this record
itself states above. The metal is the remainder: of that tie's 8.601 Ohm shared
leg, 6.82 Ohm is two single via cuts (`rcvia2` and `rcvia3` = 3.41 Ohm each, from
the same file's `pdk_resistances`) against 1.781 Ohm of met2.

Read the bullet as: the arrays divide the **via** half of each ladder and leave
that met2 remainder untouched, which is why a fully arrayed tie still cannot
reach the `0x` rung. The generator line that mis-attributed the number is fixed
in `sim/supply-impedance-sensitivity/run_supply_impedance.py` under this same
issue, so a re-run cannot reproduce it.
