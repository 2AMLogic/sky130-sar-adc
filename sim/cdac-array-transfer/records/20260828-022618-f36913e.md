# Monte Carlo record 20260828-022618-f36913e

- **Record ID**: 20260828-022618-f36913e
- **Claim**: `spec/target-spec.md#target-table` -- INL/DNL row, re-scored against a CANDIDATE REVISED target (2 LSB, target_yield=0.99) that issue #129's `spec/decision-records/DR-007-*.md` proposes (evidence-derived, per #29's shortfall against the original DRAFT `<= +-1 LSB` row -- see that record). This record supplies that re-scoring: the SAME underlying DNL/INL draws, re-evaluated against the candidate revised bound, informs (does not by itself ratify) DR-007's proposal.
- **Reanalysis, not a new simulation**: derived/composite -- NO new ngspice invocation. Re-parses the already-committed `mc-draws/20260828-005006-0c70212/` logs from source record `sim/cdac-array-transfer/records/20260828-005006-0c70212.md` (issue #29) with the SAME DNL/INL computation `_parse_draw()` uses, then re-runs `klt yield` against the candidate revised limit above -- mirrors `sim/enob-estimate/run_enob.py`'s own 'derived/composite' provenance convention.
- **Statistical convention**: mismatch corner `tt_mm`, N=40, seed=1 (draws use seed, seed+1, ..., seed+N-1), PVT point process=tt temp=27.0C supply=1.8V. **Subset-corner justification**: nominal PVT point only -- a full corner x mismatch sweep multiplies this campaign's cost by the corner count for a second-order refinement (mismatch sigma varies only weakly with PVT relative to its own draw-to-draw spread); combining with the item-5 PVT corner evidence (issue #28, sim/cdac-array-transfer/records/20260827-213107-e13bc1e.md) covers the PVT axis for this same DUT (structural sizing + monotonicity across the full ratified corner set) -- this record adds the mismatch axis at the nominal corner rather than re-deriving both simultaneously.
- **N justification**: target relative standard error on the estimated stdev <= 10%, via the standard result SE(s)/s ~= 1/sqrt(2(N-1)) for an approximately-Gaussian per-draw statistic; N=40 gives 11.3%, meeting that target. This is NOT a sample size adequate for a tight (+-1 percentage point) yield-fraction claim at 95% confidence (that needs O(100s), see the klt yield sample-size verdict below) -- N is sized for the distribution-shape claim this record makes, not chosen for runtime convenience (stated honestly per sim/README.md, matching the sim/comparator-decision/ offset record's own convention).
- **Linearity methodology**: `reduced-code-set-major-carry`, extended Monte Carlo variant of #53's coarse spot-check -- 22 codes ([0, 1, 2, 3, 4, 7, 8, 15, 16, 31, 32, 63, 64, 127, 128, 255, 256, 383, 384, 385, 510, 511]), covering every one of the 9-bit sub-array's major-carry PAIRS (13 adjacent-code transitions this set can compute a real DNL for: [(0, 1), (1, 2), (2, 3), (3, 4), (7, 8), (15, 16), (31, 32), (63, 64), (127, 128), (255, 256), (383, 384), (384, 385), (510, 511)]), the code range's endpoints, and a short consecutive run near code=0. NOT the full 512-code ramp (DNL/INL are reported ONLY at the codes/transitions listed, not interpolated or assumed elsewhere).
- **UNITS / scope note**: this array's own ideal code-to-code step is 7.0312 mV = 2x the ratified ADC LSB (3.5156 mV, DR-003 via #27) -- the array realizes only the 9-bit sub-array's own 512 differential levels; the ratified N=10 total resolution's tenth bit comes from the top-level differential sign structure (design/sar_adc_top.spice), which this array-only experiment does not exercise. DNL/INL below are reported in units of the RATIFIED ADC LSB (matching spec/target-spec.md's own row), not this array's own 7.03 mV step -- see sim/cdac-array-transfer/run_mc.py's module docstring.
- **Negative control**: N=40 draws at the plain `tt` corner (mismatch DISABLED), same seed sequence -- PASS (stdev == 0 on both DNLmax and INLmax)
- **Positive control**: N=40 draws at the `tt_mm` corner (mismatch ENABLED) -- DNLmax stdev=0.308322 LSB, INLmax stdev=0.205799 LSB (> 0, shows genuine spread -- PASS)
- **Overall**: PASS (harness/negative-control validity; DNL/INL magnitude itself is reported informationally below against the DRAFT target, not gated as pass/fail -- the target row is not yet ratified)

## DNL/INL distributions (mismatch-enabled draws, worst-case per draw across the sampled code set)

| statistic | N | mean (LSB) | stdev (LSB) | min (LSB) | max (LSB) |
|---|---|---|---|---|---|
| max\|DNL\| | 40 | 0.7833 | 0.3083 | 0.3275 | 1.9716 |
| max\|INL\| | 40 | 0.7191 | 0.2058 | 0.3818 | 1.3147 |

## Negative control (mismatch-disabled, same seed sequence)

| statistic | N | mean (LSB) | stdev (LSB, must be 0) |
|---|---|---|---|
| max\|DNL\| | 40 | 0.2364 | 0 |
| max\|INL\| | 40 | 0.1534 | 0 |

## Machine-checkable yield evidence (`klt yield`)

`klt yield` did not produce a report in this environment (either `klt` is not on PATH, or its native `klt_yield_native` extension is not built -- see `docs/cli/yield.md#building-the-native-extension` in 2AMLogic/klayout-tools; issue 2AMLogic/klayout-tools#1061 already tracks this as a known packaging gap, closed COMPLETED, with the documented build-from-checkout remediation this repo's own evidence-generation environment used when this record was produced). This is a known, already-filed environment gap, not a new one filed by this record.

No spec row is relaxed to make this result pass or fail -- the target above is quoted verbatim from its source (spec/target-spec.md's DRAFT row, or DR-007's candidate revised proposal) and explicitly labeled DRAFT/candidate throughout, per CLAUDE.md's 'do not invent settled numbers to replace the drafts' rule.

## Environment

- PDK: sky130A @ c6d73a35f524070e85faff4a6a9eef49553ebc2b
- ngspice: ngspice-47
- Harness: sim/harness 0.1.0
- git: `f36913e53fdc72dbdaea3c6fa360c92c150ac76b` on `feature/issue-129` (dirty)
- DUT netlist sha256: `ced302c25f660c9434a065471dc8f8ab9c5c848d955c8ed6f5bf52043041c3e6`
- MC seed: 1
- MC N: 40
- Reanalysis of: `sim/cdac-array-transfer/records/20260828-005006-0c70212.md` (no new ngspice run)

- **Supersedes**: (none)

Written by `sim/cdac-array-transfer/run_mc.py`. Append-only: never edit or delete this file -- a re-run or correction mints a new record-id and points back here via **Supersedes** (see `sim/README.md`).
